"use strict";

const {
  join
} = require('path');

const {
  existsSync
} = require('fs');

module.exports = function getLayer0ConfigFilePath() {
  return [join(process.cwd(), 'layer0.config.js'), join(process.cwd(), 'layer0.config.cjs')].find(existsSync);
};