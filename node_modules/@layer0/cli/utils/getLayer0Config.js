"use strict";

const slash = require('slash');

const getLayer0ConfigFilePath = require('./getLayer0ConfigFilePath');

module.exports = function getLayer0Config() {
  const file = getLayer0ConfigFilePath();

  if (file) {
    return require(slash(file));
  } else {
    return null;
  }
};