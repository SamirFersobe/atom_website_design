import { BuildOptions } from 'esbuild';
/**
 * Creates a serverless bundle using ESBuild
 * @param options Options for ESBuild
 */
export declare function bundle(options?: BuildOptions): Promise<void>;
/**
 * Builds the user's routes and layer0.config files.
 * @param options Options for ESBuild
 */
export declare function bundleRouterAndConfig(options?: BuildOptions): Promise<void>;
