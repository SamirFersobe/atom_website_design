/**
 * Returns either null or regex even if input is string matcher.
 *
 * @param potentialRegex
 * @private
 */
export default function convertStringMatcherToRegex(potentialRegex: string | RegExp | null): RegExp;
