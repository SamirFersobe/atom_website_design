"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * `set` function instead of importing lodash.
 *
 * Note that this function is not entirely equivalent to lodash's but it uses far less space in the bundle.
 */
function set(object, path, value) {
    const components = path.split('.');
    let subObject = object;
    for (let i = 0; i < components.length - 1; ++i) {
        const component = components[i];
        if (typeof subObject[component] !== 'object') {
            subObject[component] = {};
        }
        subObject = subObject[component];
    }
    subObject[components.pop()] = value;
    return object;
}
exports.default = set;
