/**
 * Matches the edge cache-control header in the response with an optional duration
 *
 * **Example**
 * ```js
 * expect(response).toBeCachedByTheEdge()
 * ...
 * expect(response).toBeCachedByTheEdge(60*60*24)
 * ```
 *
 * @param {MockResponse} received The mocked response
 * @param {Number} duration Optional - the duration (in seconds) the response is expected to be cached for
 * @returns
 */
export declare function toBeCachedByTheEdge(received: any, duration?: number): {
    message: () => string;
    pass: boolean;
};
export declare const toBeCachedByTheEdgeFor: typeof toBeCachedByTheEdge;
