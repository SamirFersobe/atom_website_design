/**
 * Matches the browser cache-control header in the response with an optional duration
 *
 * **Example**
 * ```js
 * expect(response).toBeCachedByTheBrowser()
 * ...
 * expect(response).toBeCachedByTheBrowser(60*60*24)
 * ```
 *
 * @param {MockResponse} received The mocked response
 * @param {Number} duration Optional - the duration (in seconds) the response is expected to be cached for
 * @returns
 */
export declare function toBeCachedByTheBrowser(received: any, duration?: number): {
    message: () => string;
    pass: boolean;
};
export declare const toBeCachedByTheBrowserFor: typeof toBeCachedByTheBrowser;
