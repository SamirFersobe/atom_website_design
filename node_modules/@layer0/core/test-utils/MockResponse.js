"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// @ts-nocheck
class MockResponse {
    constructor(config = {}) {
        this.headers = {
            'x-test': 'jest',
        };
        this.clone = () => this;
        Object.assign(this, {
            writeHead: jest.fn(),
            end: jest.fn(body => (this.body = (body || '').toString())),
            getHeader: jest.fn(name => this.headers[name.toLowerCase()] || this.headers[name]),
            setHeader: jest.fn((name, value) => (this.headers[name] = value)),
            removeHeader: jest.fn(name => delete this.headers[name]),
            getHeaders: () => this.headers,
            on: jest.fn(),
            once: jest.fn(),
            write: jest.fn(),
            emit: jest.fn(),
        }, config);
    }
}
exports.default = MockResponse;
