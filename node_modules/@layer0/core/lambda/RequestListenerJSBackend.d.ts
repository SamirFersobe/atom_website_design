/// <reference types="node" />
import JSBackend from './JSBackend';
import { RequestListener } from 'http';
export default class RequestListenerJSBackend extends JSBackend {
    private listener;
    constructor(port: number, listener: RequestListener);
    doStart(): Promise<void>;
}
