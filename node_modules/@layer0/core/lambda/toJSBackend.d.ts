/// <reference types="node" />
import { RequestListener } from 'http';
import JSBackend from './JSBackend';
/**
 * Normalizes the provided server as a JSBackend by returning it if it's already an instance of
 * JSBackend, or wrapping it in RequestListenerJSBackend if it's a RequestListener
 * @param server
 * @param port
 */
export default function toJSBackend(server: RequestListener | typeof JSBackend, port: number): JSBackend;
