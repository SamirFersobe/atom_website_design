"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const JSBackend_1 = __importDefault(require("./JSBackend"));
const RequestListenerJSBackend_1 = __importDefault(require("./RequestListenerJSBackend"));
/**
 * Normalizes the provided server as a JSBackend by returning it if it's already an instance of
 * JSBackend, or wrapping it in RequestListenerJSBackend if it's a RequestListener
 * @param server
 * @param port
 */
function toJSBackend(server, port) {
    if (JSBackend_1.default.isSubclass(server)) {
        // @ts-ignore
        return new server(port);
    }
    else {
        return new RequestListenerJSBackend_1.default(port, server);
    }
}
exports.default = toJSBackend;
