/**
 * Layer0 configuration file name.
 */
export declare const LAYER0_CONFIG_FILE = "layer0.config.js";
/**
 * Indicate asset files that need to be uploaded under a different
 * bucket key.
 * It contains values like
 * {
 *   '/posts' => '/posts/index.html'
 * }
 */
export declare const LAYER0_ASSET_ALIASES_FILE = "asset-aliases.json";
/**
 * Environment variables used by Layer0.
 */
export declare const LAYER0_ENV_VARIABLES: {
    /**
     * Layer0 configuration environment variable key.
     */
    config: string;
    /**
     * Indicates whether code is running locally or in the cloud.
     * @private
     */
    deploymentType: string;
    /**
     * Allows Layer0 development team to easily override Layer0 version
     * during development process.
     * @private
     */
    versionOverride: string;
};
/**
 * Indicates that code is running in AWS.
 * @private
 */
export declare const LAYER0_DEPLOYMENT_TYPE_AWS = "AWS";
/**
 * Prefix of all Layer0 headers.
 */
export declare const LAYER0_HEADERS_PREFIX = "x-0-";
/**
 * Maximum number of user headers allowed by Layer0 platform.
 *
 * We limit the request and response headers allowed to 70. This is due to a limitation that edge has
 * which at the moment supports up to 96 headers. Edge provider itself needs to use some of those headers
 * with the remainder used by our edge code.
 *
 * NOTE: This constant must be equal ot the constant of the same name defined in Layer0 buffer proxy.
 * NOTE: If this constant is updated then the constant of the same name has to be updated in @layer0/build-lambda module.
 */
export declare const LAYER0_MAX_USER_HEADERS_ALLOWED = 70;
/**
 * Status code returned when there are too many request or response headers.
 *
 * Must be equal to HeaderOverflowError status code as defined in build lambda and Layer0 buffer proxy.
 */
export declare const LAYER0_TOO_MANY_HEADERS_STATUS_CODE = 542;
/**
 * The status code returned when the layer0_prefetch query param is present and a response could not be found in the cache.
 */
export declare const LAYER0_UNCACHED_PREFETCH_STATUS = 412;
/**
 * CDN-as-code configuration actions
 */
export declare const ACTIONS: {
    setHeader: string;
    updateHeader: string;
    removeHeader: string;
    syntheticRes: string;
    updatePath: string;
    proxy: string;
    addCookie: string;
    updateCookie: string;
    removeCookie: string;
};
/**
 * The backend for cloud functions
 */
export declare const BACKENDS: {
    js: string;
    static: string;
    permanentStatic: string;
    imageOptimizer: string;
};
/**
 * The hostname used for the internal service running the user's application.
 */
export declare const JS_BACKEND_HOSTNAME = "127.0.0.1";
export declare const HTTP_METHODS: {
    head: string;
    get: string;
    post: string;
    delete: string;
    put: string;
    patch: string;
    options: string;
};
/**
 * Common HTTP headers.
 */
export declare const HTTP_HEADERS: {
    authorization: string;
    acceptEncoding: string;
    cacheControl: string;
    contentEncoding: string;
    contentLength: string;
    contentType: string;
    cookie: string;
    host: string;
    location: string;
    serverTiming: string;
    setCookie: string;
    userAgent: string;
    vary: string;
    via: string;
    xForwardedFor: string;
    xRequestId: string;
    xSwCacheControl: string;
    x0Browser: string;
    x0CacheControl: string;
    x0CachingStatus: string;
    x0ClientIp: string;
    x0Components: string;
    x0Destination: string;
    x0Device: string;
    x0DeviceIsBot: string;
    x0GeoCity: string;
    x0GeoCountryCode: string;
    x0GeoLatitude: string;
    x0GeoLongitude: string;
    x0GeoPostalCode: string;
    x0MatchedRoutes: string;
    x0Protocol: string;
    x0Route: string;
    x0Status: string;
    x0SurrogateKey: string;
    x0T: string;
    x0UserT: string;
    x0Vendor: string;
    x0Version: string;
};
/**
 * Values for x-0-caching-status
 */
export declare const CACHING_STATUS: {
    ok: string;
    private: string;
    method: string;
    bodyTooBig: string;
    code: string;
    setCookie: string;
    noMaxAge: string;
};
export declare const CACHEABLE_METHODS: Set<string>;
/**
 * When present, this query parameter will cause Layer0 to return a 412 status
 * if a response could not be found in the cache.
 */
export declare const THROTTLED_QUERY_PARAM = "layer0_prefetch";
/**
 * Same value as above but DEPRECATED.
 */
export declare const PREFETCH_QUERY_PARAM = "layer0_prefetch";
/**
 Adds query parameter with custom maxAgeSecond value for service-worker cache if it's specified in PrefetchConfiguration
 */
export declare const PREFETCH_TTL_PARAM = "layer0_prefetch_ttl";
/**
 Labels the request so that devtools will display as prefetched
 */
export declare const DEVTOOLS_PREFETCH_QUERY_PARAM = "layer0_dt_pf";
/**
 * The name of the query parameter used to detect HEAD requests.
 */
export declare const HEAD_QUERY_PARAM = "layer0_head";
/**
 * The name of the query parameter used for the body of POST requests, which is used for the cache key
 */
export declare const POST_BODY_QUERY_PARAM = "pref_layer0_body";
/**
 * The name of the query parameter used for the method of requests, which is used for the cache key
 */
export declare const METHOD_QUERY_PARAM = "pref_layer0_method";
/**
 * Route Group name, which is used in router for fallback routes
 */
export declare const ROUTES_FALLBACK = "fallback";
/**
 * Route Group name, which is used in router for error page routes
 */
export declare const ROUTES_CATCH_GROUP = "catch";
/**
 * Route Group name, which is used in router for noindex routes
 */
export declare const ROUTES_NOINDEX_GROUP = "noindex";
/**
 * The path for the built-in image optimizer.
 */
export declare const LAYER0_IMAGE_OPTIMIZER_PATH = "/__layer0_image_optimizer";
/**
 * Is current environment 'browser'
 */
export declare const IS_BROWSER: boolean;
/**
 * Brotli encoding code.
 */
export declare const BROTLI_ENCODING = "br";
/**
 * Gzip encoding code.
 */
export declare const GZIP_ENCODING = "gzip";
