import { Router } from '../router';
import { EdgeBodyMatcher, EdgeRegexMatcher, EdgeConfig } from '../types';
export interface BuildMatcherFromRouteResult {
    match: (EdgeRegexMatcher | EdgeBodyMatcher)[];
    matchResp: EdgeRegexMatcher[];
    matchExplicitMethod?: string;
    matchProtocolJavaScriptRegExpSource?: string;
}
/**
 * Creates an outer edge manager config for the specified router by iterating through
 * each route and calling it with a mock request
 * and response.
 * @param router
 * @return An object containing the Edge config
 */
export default function createEdgeConfig(router: Router): Promise<EdgeConfig>;
