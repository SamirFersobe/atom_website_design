import { ProxyOptions } from '../router/ResponseWriter';
import EdgeResponseWriter from './EdgeResponseWriter';
export default class EdgeErrorPageResponseWriter extends EdgeResponseWriter {
    /**
     * Disables proxy from the edge for error pages
     *
     * @param backend
     * @param config
     * @param config.path
     */
    proxy: (_backend: string, _opts?: ProxyOptions) => never;
}
