import RouteCriteria from '../router/RouteCriteria';
interface RouteInfo {
    criteria: RouteCriteria;
    line: number;
    column: number;
}
/**
 * Parses and introspects the user's routes file.
 * @private
 * @param routerFile
 */
export default function parseRouter(routerFile: string): RouteInfo[];
export {};
