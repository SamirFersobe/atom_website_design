/**
 * To be thrown when the user's application fails to build.
 */
export default class FrameworkBuildError extends Error {
    readonly isUserError = true;
    constructor(framework: string, command: string, error?: any);
}
