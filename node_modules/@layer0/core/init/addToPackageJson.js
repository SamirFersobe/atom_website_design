"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const fs_1 = require("fs");
const deepmerge_1 = __importDefault(require("deepmerge"));
/**
 * Merges the provided config into package.json and writes the result to disk.
 * @param contents The config to add
 * @param overwrite True to overwrite any values that already exist in the package.json file
 * @return The resulting package.json contents as an object
 */
function addToPackageJson(contents, overwrite = true) {
    const file = path_1.join(process.cwd(), 'package.json');
    let packageContents = JSON.parse(fs_1.readFileSync(file, 'utf8'));
    packageContents = overwrite ? deepmerge_1.default(packageContents, contents) : deepmerge_1.default(contents, packageContents);
    fs_1.writeFileSync(file, JSON.stringify(packageContents, null, 2), 'utf8');
    return packageContents;
}
exports.default = addToPackageJson;
